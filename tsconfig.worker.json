{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    // Lets the Apple modules name each other with a `.ts` suffix, which is
    // what makes them loadable by the node:test runner as well as the bundler.
    "allowImportingTsExtensions": true,
    "lib": ["ES2022"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "isolatedModules": true,
    "types": []
  },
  "include": ["src/worker/**/*.ts", "src/lib/**/*.ts", "worker-configuration.d.ts"],
  "exclude": ["src/worker/**/*.test.ts"]
}
