{
  "name": "zenbuy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy --config dist/zenbuy/wrangler.json",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc -p tsconfig.json && tsc -p tsconfig.worker.json",
    "check": "wrangler check",
    "smoke": "node tools/smoke-test.mjs",
    "test": "node --experimental-strip-types --test src/worker/parse.test.ts src/client/processing-progress.test.ts src/client/unlock-copy.test.ts src/worker/apple-jws.test.ts src/worker/quota.test.ts src/worker/unlock.test.ts src/worker/prompt.test.ts",
    "test:parse": "npm test"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.12.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "wrangler": "^4.33.0"
  }
}
